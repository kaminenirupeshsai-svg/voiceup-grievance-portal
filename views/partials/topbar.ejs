<header class="app-topbar">
  <div>
    <h1><%= typeof pageTitle !== "undefined" ? pageTitle : "" %></h1>
  </div>
  <div class="sub"><%= typeof pageSub !== "undefined" ? pageSub : "" %></div>
</header>
